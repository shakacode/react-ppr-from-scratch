#!/usr/bin/env bash
# Generated by shakacode/agent-workflows agent-workflow-seam-doctor --init.
set -euo pipefail
cd "$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)"

if [ "$#" -ne 0 ]; then
  printf '%s\n' "Usage: .agents/bin/build" >&2
  printf '%s\n' "This wrapper does not accept arguments; run npm directly for custom build options." >&2
  exit 64
fi

.agents/bin/setup
exec npm run build
